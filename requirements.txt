numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
biopython>=1.81
tqdm>=4.65
pytest>=7.4

# Optional, for the protein-language-model embedding baseline.
# Requires a GPU (or patience) and internet access to fetch pretrained weights.
# fair-esm>=2.0.0
# torch>=2.0
